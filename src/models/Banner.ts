export interface BannerType {
    image: string,
    title: string, 
    description: string, 
    phrase?: string,
    phraseText?: string,
    side: string,
    background:string,
    button?: string,
    buttonText?: string,
    buttonStyle?: string,
}